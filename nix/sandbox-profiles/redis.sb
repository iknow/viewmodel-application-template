(version 1)

(deny default)

(import "@common@")

@helpers@

;; This is a network server, it binds and accepts connections
(allow network-bind network-inbound (local tcp))

;; Redis forks in order to background save
(allow process-fork)

;; zmalloc wants to inspect memory usage
(allow mach-priv-task-port (target self))
